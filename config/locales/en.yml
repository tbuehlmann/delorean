en:
  breadcrumbs:
    projects:
      index: Projects
      create: Create Project
      update: Update Project
    issues:
      index: Issues
      create: Create Issue
      update: Update Issue
    records:
      index: Work
      create: Log Work
      update: Update Work
  activerecord:
    models:
      record: Work
  helpers:
      submit:
        record:
          create: "Log %{model}"
